# 个人生活记录网页开发说明文档

> 用途：本 `.md` 文件用于指导 Claude Code 生成一个“个人生活记录 / 照片回忆网页”。  
> 目标：制作一个简洁、美观、温暖、有生活感的个人网页，用来展示和朋友出去玩时拍摄的照片、旅行记录、生活片段和文字回忆。

---

## 1. 项目目标

请帮我开发一个个人生活记录网页，主题偏向：

- 记录生活
- 展示和朋友出去玩的照片
- 保存旅行、聚会、校园生活、日常瞬间
- 可以配简单文字说明
- 风格温暖、干净、治愈，不要太商业化

网页整体像一个个人相册、生活日记、回忆墙，而不是正式的学术主页或企业官网。

---

## 2. 技术要求

请优先使用以下技术实现：

- HTML
- CSS
- JavaScript

要求：

1. 项目结构清晰，适合新手修改。
2. 不要使用复杂后端。
3. 网页可以直接在浏览器中打开运行。
4. 所有照片从本地文件夹读取，例如 `assets/images/`。
5. CSS 和 JavaScript 分离，便于后续维护。
6. 页面需要适配电脑端和手机端。

推荐项目结构如下：

```text
life-website/
├── index.html
├── README.md
├── assets/
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   └── main.js
│   └── images/
│       ├── cover.jpg
│       ├── avatar.jpg
│       ├── trip-01.jpg
│       ├── trip-02.jpg
│       └── ...
```

---

## 3. 网页整体风格

网页风格希望是：

- 温暖
- 简洁
- 治愈
- 有生活气息
- 带一点相册感和日记感

视觉建议：

- 背景颜色：米白色、浅灰色、淡蓝色、浅奶油色均可。
- 主色调：柔和蓝、暖橙、浅咖色、淡绿色均可。
- 字体：中文可以使用 `Microsoft YaHei`、`PingFang SC`、`Noto Sans SC`。
- 卡片需要有圆角和轻微阴影。
- 图片展示时需要有圆角，鼠标悬停时可以轻微放大。
- 整体不要太花哨，不要使用过多动画。

---

## 4. 页面结构设计

整个网页只需要一个首页 `index.html`，采用单页滚动布局。

页面包含以下几个部分：

1. 顶部导航栏
2. 首页封面区域
3. 关于这个网页
4. 照片回忆墙
5. 时间线记录
6. 朋友与瞬间
7. 留言 / 心情记录区域
8. 页脚

---

## 5. 顶部导航栏

导航栏固定在页面顶部或普通显示均可。

导航栏包含以下菜单：

- 首页
- 关于
- 相册
- 时间线
- 瞬间
- 留言

点击菜单后，可以平滑滚动到对应区域。

要求：

- 电脑端横向排列。
- 手机端可以自动换行，或者变成简单的竖向菜单。
- 导航栏背景可以是半透明白色或纯白色。

---

## 6. 首页封面区域

首页顶部需要一个大封面区域，可以使用 `assets/images/cover.jpg` 作为背景图。

封面区域内容包括：

```text
我的生活小站
把普通日子里的开心瞬间，慢慢收藏起来。
```

可以加一个按钮：

```text
查看我的回忆
```

点击按钮后滚动到照片墙区域。

要求：

- 封面图上方可以加一层半透明遮罩，让文字更清晰。
- 文字居中显示。
- 整体有温暖、回忆感。

---

## 7. 关于这个网页区域

这一部分用于简单介绍网页用途。

示例文字：

```text
这里是我的生活记录网页，用来保存一些和朋友出去玩、旅行、聚会、校园生活以及日常碎片的照片和文字。很多瞬间可能很普通，但过一段时间再回头看，都会变成很珍贵的回忆。
```

可以配一张个人头像或生活照片，例如：

```text
assets/images/avatar.jpg
```

布局建议：

- 左边文字，右边图片。
- 手机端上下排列。

---

## 8. 照片回忆墙区域

这是网页的核心部分。

请制作一个照片卡片网格，用来展示多张照片。

每张照片卡片包含：

- 图片
- 标题
- 日期
- 简短描述
- 标签

示例数据：

```javascript
const photos = [
  {
    src: "assets/images/trip-01.jpg",
    title: "和朋友的周末出行",
    date: "2025-05-01",
    desc: "天气很好，大家一起出去走了很久，也拍了很多照片。",
    tag: "朋友 / 出游"
  },
  {
    src: "assets/images/trip-02.jpg",
    title: "海边的一天",
    date: "2025-07-18",
    desc: "吹海风、看日落，是很放松的一天。",
    tag: "旅行 / 海边"
  },
  {
    src: "assets/images/trip-03.jpg",
    title: "校园里的合照",
    date: "2025-09-10",
    desc: "普通的一天，但和朋友在一起就很开心。",
    tag: "校园 / 日常"
  }
];
```

功能要求：

1. 照片以卡片形式展示。
2. 每行根据屏幕宽度自动显示 1 到 3 张。
3. 鼠标移动到照片上时，图片轻微放大。
4. 点击图片后，可以弹出大图预览。
5. 大图预览中显示图片标题和描述。
6. 预览窗口可以点击关闭按钮关闭，也可以点击背景关闭。

---

## 9. 照片分类筛选功能

请给照片墙增加简单分类筛选按钮。

分类包括：

- 全部
- 朋友
- 旅行
- 校园
- 日常
- 美食

点击分类按钮后，只显示对应类别的照片。

示例：

```javascript
category: "朋友"
```

如果照片数据中的类别为“朋友”，点击“朋友”按钮时显示该照片。

要求：

- 当前选中的分类按钮需要高亮。
- 筛选时不要刷新页面。
- 使用 JavaScript 实现即可。

---

## 10. 时间线记录区域

制作一个简单时间线，用来记录一些重要生活片段。

示例内容：

```text
2025.04  第一次和朋友一起短途旅行
2025.06  一起拍了毕业季照片
2025.08  去海边看了一次日落
2025.10  周末聚餐，聊了很多近况
```

设计要求：

- 左侧显示时间，右侧显示事件内容。
- 或者使用竖向时间线样式。
- 每个时间点是一个圆点，连接成一条线。
- 风格简洁，不要太复杂。

---

## 11. 朋友与瞬间区域

这一部分可以展示一些小卡片，每张卡片记录一种生活瞬间。

示例卡片：

1. 一起走过的路
2. 一起吃过的饭
3. 一起拍过的照片
4. 一起聊过的晚上

每张卡片包含一个 emoji 图标、标题和一句描述。

示例：

```text
🍜 一起吃过的饭
有些饭局不只是吃饭，也是很多开心记忆的开始。
```

---

## 12. 留言 / 心情记录区域

制作一个简单的留言或心情记录区域。

不需要真正连接数据库，只需要前端展示即可。

可以包含：

- 一个输入框：写下一句话
- 一个按钮：添加记录
- 添加后显示在页面中

要求：

1. 使用 JavaScript 实现本地添加。
2. 可以使用 `localStorage` 保存留言，这样刷新页面后留言还在。
3. 每条留言显示添加时间。
4. 提供删除单条留言的按钮。

示例留言：

```text
今天也拍到了很喜欢的照片。
希望以后还能和朋友去更多地方。
```

---

## 13. 页脚区域

页脚内容示例：

```text
© 2026 我的生活小站 | 记录普通但珍贵的日子
```

可以加一句：

```text
Made with memories and photos.
```

---

## 14. 交互功能总结

请至少实现以下交互：

1. 导航栏平滑滚动。
2. 照片分类筛选。
3. 点击照片弹出大图预览。
4. 关闭大图预览。
5. 留言添加功能。
6. 留言保存到 `localStorage`。
7. 留言删除功能。
8. 手机端自适应布局。

---

## 15. 文件内容要求

请生成完整代码，不要只给片段。

需要生成以下文件：

```text
index.html
assets/css/style.css
assets/js/main.js
README.md
```

每个文件要求：

### index.html

- 页面主体结构完整。
- 正确引入 CSS 和 JS。
- 所有区域都要有对应 `id`，方便导航跳转。

### assets/css/style.css

- 完整页面样式。
- 包含响应式布局。
- 包含照片卡片、按钮、弹窗、时间线、留言区域等样式。

### assets/js/main.js

- 存放照片数据。
- 渲染照片墙。
- 实现分类筛选。
- 实现图片预览弹窗。
- 实现留言添加、保存和删除。

### README.md

需要说明：

1. 项目用途。
2. 如何运行。
3. 如何替换照片。
4. 如何新增照片数据。
5. 如何修改网页标题和文字。

---

## 16. 图片命名建议

请在代码中默认使用以下图片路径：

```text
assets/images/cover.jpg
assets/images/avatar.jpg
assets/images/trip-01.jpg
assets/images/trip-02.jpg
assets/images/trip-03.jpg
assets/images/trip-04.jpg
assets/images/trip-05.jpg
assets/images/trip-06.jpg
```

如果图片不存在，也不要让页面完全崩溃。可以使用背景色或默认占位提示。

---

## 17. 照片数据格式要求

请在 `main.js` 中使用数组统一管理照片数据，方便我以后修改。

数据格式如下：

```javascript
const photos = [
  {
    src: "assets/images/trip-01.jpg",
    title: "照片标题",
    date: "2025-05-01",
    desc: "这里写这张照片背后的故事。",
    category: "朋友",
    tag: "朋友 / 出游"
  }
];
```

以后我只需要复制一段对象，就可以新增照片。

---

## 18. 设计细节要求

1. 页面顶部封面要有视觉冲击，但不要太复杂。
2. 所有卡片统一圆角。
3. 图片比例尽量统一，避免大小混乱。
4. 页面留白要舒服。
5. 颜色不要太刺眼。
6. 字体大小适中，手机端也要清楚。
7. 动画只需要轻微过渡，例如 hover 放大、按钮变色、弹窗淡入。
8. 代码注释适当添加，方便我后续修改。

---

## 19. 可选增强功能

如果基础功能完成后，可以继续增加以下功能：

1. 夜间模式切换。
2. 按日期排序照片。
3. 给照片增加地点字段。
4. 增加“年度回忆”区域。
5. 增加背景音乐开关，但默认不要自动播放。
6. 增加照片瀑布流布局。
7. 增加回到顶部按钮。

基础版本优先完成，不要因为增强功能导致代码过于复杂。

---

## 20. 隐私与安全要求

由于网页可能包含朋友照片，需要注意：

1. 不要在代码中写真实手机号、身份证号、详细住址等隐私信息。
2. 如果以后部署到公网，照片需要提前确认是否适合公开。
3. 可以在 README 中提醒我：公开前检查照片和文字内容。

---

## 21. Claude Code 执行指令

请 Claude Code 根据本说明文档，直接生成完整项目代码。

要求：

1. 先创建项目目录 `life-website`。
2. 按照说明创建完整文件结构。
3. 生成完整的 `index.html`、`style.css`、`main.js` 和 `README.md`。
4. 页面能够直接通过浏览器打开 `index.html` 运行。
5. 不要省略代码。
6. 不要只给示例片段。
7. 所有功能必须尽量完整可用。
8. 如果图片不存在，需要使用友好的占位方式处理。

最终请输出：

- 项目文件结构
- 每个文件的完整代码
- 如何运行网页
- 如何替换照片
- 如何新增照片卡片

---

## 22. 期望最终效果

最终网页打开后，应具有以下效果：

1. 顶部有一张生活封面图和网页标题。
2. 页面整体温暖、干净、美观。
3. 可以看到照片墙，每张照片有标题、日期、描述和标签。
4. 可以按“朋友、旅行、校园、日常、美食”等类别筛选照片。
5. 点击照片可以放大查看。
6. 有一个时间线记录生活事件。
7. 有一个简单留言区域，可以写下心情并保存。
8. 手机和电脑上都能正常显示。

---

## 23. 后续我会自己替换的内容

以下内容请在代码中保留为容易修改的形式：

- 网页标题
- 首页标语
- 关于区域文字
- 封面图
- 头像图
- 照片数据数组
- 时间线内容
- 页脚文字

请尽量把可修改内容集中在 HTML 或 JS 的明显位置，并添加注释。

